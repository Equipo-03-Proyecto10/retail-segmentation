# ADR-NNNN — <the decision, stated as a decision rather than as a topic>

**Status:** Proposed
**Owner:** <name>
**Issue:** <the issue that produced this decision>
**Supersedes:** — *(or ADR-NNNN)*
**Superseded by:** — *(filled in only when this record is replaced)*

---

## Context

What forces this decision now: the constraint, the requirement, or the conflict
that made a decision unavoidable. Include what is genuinely uncertain — a record
that reads as though the answer was obvious is suspicious, and the reader needs
to know which parts were judgement calls.

## Decision

The decision, present tense, active voice. One paragraph. If it takes three, the
record is carrying more than one decision and should be split.

## Alternatives considered

| Alternative | Why it was rejected |
|---|---|
| <the option a reasonable person would have picked> | <the specific reason, not "it was worse"> |

At least one row.

## Consequences

**What this makes easy.**

**What this makes hard.** Be honest. Every decision has a cost, and the cost is
the part a future reader needs, because they will be paying it.

**What must now be true elsewhere.** Other documents or stories that inherit
this decision and would break if it changed.

## Compliance

How a reviewer checks that an implementation actually follows this record — a
test, a constraint, a script, a query. Something executable where possible.
"By code review" is acceptable only when nothing better exists.
