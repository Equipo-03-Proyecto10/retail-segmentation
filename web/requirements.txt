# Application runtime dependencies.
#
# psycopg is used directly rather than through an ORM: the exercise grades a
# normalized schema and parameterized SQL, and an ORM hides both. Versions are
# confirmed when F3-01 builds the application skeleton.

Flask==3.1.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
argon2-cffi==23.1.0
gunicorn==23.0.0
